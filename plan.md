# 미니 프론트엔드·백엔드 통합 개발 계획서

## 1. 문서 목적

현재 작업공간의 두 프로젝트인 Streamlit 프론트엔드와 FastAPI 백엔드를 하나의 서비스로 연동하기 전에, 현재 상태와 개발 범위, 구현 순서, 완료 기준을 정리한다.

이 문서는 개발 과정에서 세부 요구사항이 확정되면 지속적으로 갱신하는 기준 문서로 사용한다.

## 2. 프로젝트 현황

### 2.1 프론트엔드

- 경로: `frontend/`
- 기술: Python, Streamlit, httpx, pandas
- 실행 진입점: `frontend/app.py`
- 현재 기능:
  - 멀티페이지 내비게이션
  - 브라우저 세션 스토리지를 이용한 로그인 상태 유지
  - 고정 계정(`id01` / `pwd01`) 기반 임시 로그인
  - 회원가입 입력 화면
  - Open-Meteo API를 이용한 날씨 조회 및 차트
  - FastAPI 서버 상태 확인 화면
- 현재 한계:
  - 로그인과 회원가입이 백엔드 및 데이터베이스와 연결되어 있지 않다.
  - 로그인 상태가 실제 인증 토큰이 아닌 문자열로 관리된다.
  - API 주소가 화면 코드에 직접 작성되어 있다.
  - 백엔드의 상품 및 Gemini 채팅 API를 사용하는 화면이 없다.
  - README의 페이지 구성과 실제 파일 구성이 일치하지 않는다.

### 2.2 백엔드

- 경로: `backend/`
- 기술: Python, FastAPI, Pydantic, Uvicorn, pytest, Google GenAI SDK
- 실행 진입점: `backend/app/main.py`
- 현재 기능:
  - Gemini 단일 질문 API: `POST /chat/gemini`
  - 상품 생성 API: `POST /product/create`
  - 상품 단건 조회 API: `GET /product/get/{product_id}`
  - 상품 전체 조회 API: `GET /product/getall`
  - 상품 수정 API: `PUT /product/update/{product_id}`
  - 상품 삭제 API: `DELETE /product/delete/{product_id}`
  - 라우터 단위 기본 테스트
- 현재 한계:
  - 프론트엔드가 호출하는 `GET /health` API가 없다.
  - 상품 데이터가 메모리나 데이터베이스에 저장되지 않고 고정 응답으로 반환된다.
  - 회원가입 및 로그인 API가 없다.
  - 인증과 권한 검사가 없다.
  - Gemini API 키 누락 및 외부 API 장애에 대한 예외 처리가 부족하다.
  - 일부 서비스 함수의 타입 선언이 라우터 및 스키마와 일치하지 않는다.
  - CORS, 공통 설정, 공통 오류 응답, 로깅 정책이 아직 없다.

## 3. 개발 목표

1. 프론트엔드와 백엔드를 동일한 API 계약으로 안정적으로 연결한다.
2. 임시 로그인과 회원가입을 실제 백엔드 인증 흐름으로 교체한다.
3. 상품 CRUD를 실제 저장소와 연결하고 프론트엔드 관리 화면을 제공한다.
4. Gemini 채팅 기능을 프론트엔드에서 사용할 수 있도록 구현한다.
5. 설정, 오류 처리, 테스트, 문서를 정비해 누구나 같은 방식으로 실행하고 검증할 수 있게 한다.

## 4. 목표 구성

```text
사용자
  └─ Streamlit 프론트엔드
       ├─ 인증 화면
       ├─ 상품 관리 화면
       ├─ AI 채팅 화면
       ├─ 날씨 조회 화면
       └─ 서버 상태 화면
            │ HTTP/JSON
            ▼
       FastAPI 백엔드
       ├─ Health API
       ├─ Auth API
       ├─ Product API
       └─ Chat API ── Gemini API
            │
            ▼
       데이터 저장소
```

데이터 저장소는 초기 개발 시 메모리 또는 SQLite로 시작할 수 있으나, 운영 또는 학습 목표에 Supabase가 포함된다면 인증과 상품 데이터를 Supabase로 통합한다. 저장소 선택은 인증 구현 전에 확정한다.

## 5. 개발 원칙

- 화면에서 백엔드 URL, API 키, 비밀번호 등 환경별 값을 직접 관리하지 않는다.
- API 요청과 화면 렌더링 코드를 분리한다.
- 모든 API는 요청·응답 스키마와 오류 형식을 명확히 정의한다.
- 비밀번호 원문을 저장하거나 로그로 출력하지 않는다.
- 외부 API 호출에는 타임아웃과 사용자 친화적인 오류 처리를 적용한다.
- 각 단계는 테스트와 문서 업데이트까지 끝나야 완료로 판단한다.

## 6. 단계별 개발 계획

### 1단계: 실행 환경 및 기준 정비

작업 내용:

- 루트 기준 실행 방법과 개발 환경을 문서화한다.
- 프론트엔드와 백엔드의 Python 지원 버전을 통일한다.
- 각 프로젝트의 의존성을 실제 사용 범위에 맞게 점검한다.
- `.env.example`을 추가하고 비밀값은 `.env`에만 보관한다.
- 프론트엔드에 `API_BASE_URL` 설정을 추가한다.
- 백엔드에 애플리케이션 설정 모듈과 환경 변수 검증을 추가한다.
- 실제 파일 구조에 맞게 두 README를 수정한다.
- 백엔드에 `GET /health`를 추가해 현재 프론트 화면과 연결한다.

완료 기준:

- 새 환경에서 문서의 명령만으로 두 서버를 실행할 수 있다.
- 프론트엔드에서 백엔드 상태 확인이 성공한다.
- 저장소에 API 키와 비밀번호가 포함되지 않는다.

### 2단계: API 계약과 공통 통신 계층 정리

작업 내용:

- API 경로, HTTP 메서드, 상태 코드, 요청·응답 필드를 확정한다.
- 가능하면 `/api/v1` 공통 접두사를 적용한다.
- 상품 API를 REST 형태로 정리한다.
  - `POST /products`
  - `GET /products`
  - `GET /products/{product_id}`
  - `PUT` 또는 `PATCH /products/{product_id}`
  - `DELETE /products/{product_id}`
- 성공 응답과 오류 응답의 형식을 정의한다.
- 존재하지 않는 상품, 중복 데이터, 잘못된 입력에 적절한 HTTP 오류를 반환한다.
- 프론트엔드에 공통 API 클라이언트 모듈을 만들고 타임아웃 및 오류 변환을 한 곳에서 처리한다.
- 필요 시 CORS 허용 범위를 설정한다.

완료 기준:

- OpenAPI 문서에서 모든 API 계약을 확인할 수 있다.
- 프론트 화면 코드에 중복 HTTP 요청 코드와 하드코딩된 서버 주소가 없다.
- 기존 라우터 테스트가 새 계약에 맞게 통과한다.

### 3단계: 인증 및 회원 관리

사전 결정:

- 인증 저장소 및 방식 선택: Supabase Auth 또는 자체 회원 테이블과 토큰 방식
- 세션 만료 시간과 로그아웃 정책
- 회원 필수 항목 및 ID 중복 정책

작업 내용:

- 회원가입, 로그인, 현재 사용자 확인, 로그아웃 API를 구현한다.
- 비밀번호는 검증된 인증 서비스 또는 안전한 해시 방식으로 처리한다.
- 인증 토큰 또는 세션을 이용해 로그인 상태를 검증한다.
- 프론트엔드 회원가입 폼을 회원가입 API와 연결한다.
- 고정 계정 비교 로직을 제거하고 로그인 API로 교체한다.
- 새로고침 후 인증 복원 및 만료 시 자동 로그아웃을 처리한다.
- 인증이 필요한 페이지와 API에 접근 제어를 적용한다.
- 로그인 실패, 중복 가입, 만료 세션에 대한 안내를 추가한다.

완료 기준:

- 사용자가 가입한 계정으로 로그인하고 로그아웃할 수 있다.
- 잘못된 자격 증명과 만료된 인증 정보로 보호 자원에 접근할 수 없다.
- 비밀번호 원문이 코드, 저장소, 로그에 남지 않는다.

### 4단계: 상품 CRUD 통합

작업 내용:

- 상품 데이터 모델과 저장소 계층을 구현한다.
- 상품 ID 생성 정책, 이름 길이, 가격 범위 등 검증 규칙을 정의한다.
- 현재 고정 데이터를 반환하는 서비스를 실제 CRUD 로직으로 교체한다.
- 상품 목록, 등록, 상세, 수정, 삭제 화면을 프론트엔드에 추가한다.
- 등록·수정 폼 검증 및 삭제 확인 절차를 구현한다.
- 작업 성공 후 목록 갱신과 성공·실패 메시지를 제공한다.
- 인증된 사용자만 변경 API를 사용할 수 있도록 권한을 적용한다.

완료 기준:

- 프론트엔드에서 생성한 상품이 저장되고 재조회된다.
- 수정과 삭제 결과가 목록 및 상세 화면에 즉시 반영된다.
- 없는 상품과 잘못된 입력에 일관된 오류가 표시된다.

### 5단계: Gemini 채팅 통합

작업 내용:

- 프론트엔드에 채팅 페이지와 대화 UI를 추가한다.
- 로그인 사용자 ID를 백엔드 채팅 요청과 연결한다.
- Streamlit 세션에서 현재 대화 내역을 관리한다.
- Gemini API 키 누락, 할당량 초과, 타임아웃, 빈 응답을 백엔드에서 처리한다.
- 외부 SDK 예외를 내부 오류 응답으로 변환하고 민감 정보가 노출되지 않게 한다.
- 사용자별 대화 저장이 필요하다면 별도 대화 및 메시지 모델을 설계한다.
- 비용 및 남용 방지를 위해 요청 길이, 빈도, 최대 응답 크기를 제한한다.

완료 기준:

- 프론트엔드에서 질문을 보내고 Gemini 응답을 확인할 수 있다.
- 외부 API 장애 시 서버가 종료되지 않고 명확한 오류가 표시된다.
- Gemini 호출 테스트는 실제 네트워크 없이 모킹하여 실행할 수 있다.

### 6단계: 기존 날씨 기능 정비

작업 내용:

- 날씨 API를 프론트엔드가 직접 호출할지 백엔드를 경유할지 결정한다.
- 백엔드 경유 시 외부 API 응답 변환과 캐싱을 백엔드로 이동한다.
- 도시, 조회 기간, 데이터 단위 및 시간대 표시를 명확히 한다.
- 빈 데이터, 응답 구조 변경, 네트워크 오류를 처리한다.
- 표와 차트의 컬럼명 및 단위를 일관되게 표시한다.

완료 기준:

- 지원 도시와 조회 기간에서 표와 차트가 정상 출력된다.
- 외부 API 오류 및 예상하지 못한 응답에도 화면이 중단되지 않는다.

### 7단계: 테스트, 품질 및 운영 준비

작업 내용:

- 백엔드 서비스 및 라우터 단위 테스트를 확장한다.
- 인증, 상품 CRUD, 채팅 오류 흐름에 대한 통합 테스트를 추가한다.
- 프론트엔드의 API 클라이언트와 핵심 상태 로직을 테스트 가능한 구조로 분리한다.
- 핵심 사용자 흐름에 대한 수동 점검표 또는 E2E 테스트를 작성한다.
- 코드 포매터와 린터를 선택해 공통 규칙을 적용한다.
- 구조화된 로깅을 추가하고 `print` 기반 디버깅 출력을 제거한다.
- 운영 환경의 허용 호스트, CORS, 비밀값, 로그 레벨을 점검한다.
- 최종 README에 설치, 환경 변수, 실행, 테스트, API 사용법을 정리한다.

완료 기준:

- 전체 자동 테스트가 통과한다.
- 회원가입 → 로그인 → 상품 CRUD → 채팅 → 로그아웃의 핵심 흐름이 정상 작동한다.
- 신규 개발자가 README만 보고 로컬 환경을 재현할 수 있다.

## 7. 우선순위

### 필수(MVP)

- 실행 환경 및 환경 변수 정리
- `/health` 구현과 서버 연결 확인
- 공통 API 클라이언트
- 실제 회원가입 및 로그인
- 상품 저장 및 CRUD 화면 연동
- Gemini 채팅 화면 연동
- 핵심 API 테스트와 실행 문서

### 권장

- API 버전 관리
- 공통 오류 응답
- 대화 내역 저장
- 날씨 API 백엔드 프록시 및 캐싱
- 린트, 포맷, 테스트 자동화

### 추후 확장

- 사용자 역할 및 관리자 권한
- 상품 검색, 정렬, 페이지네이션
- 이미지 업로드
- 채팅 스트리밍 응답
- Docker 및 배포 파이프라인
- 모니터링과 사용량 통계

## 8. 예상 산출물

```text
/
├─ plan.md
├─ frontend/
│  ├─ app.py
│  ├─ app_pages/
│  ├─ core/
│  ├─ services/          # 공통 API 클라이언트
│  ├─ .env.example
│  ├─ requirements.txt
│  └─ README.md
└─ backend/
   ├─ app/
   │  ├─ main.py
   │  ├─ core/           # 설정, 보안, 공통 오류
   │  ├─ routers/
   │  ├─ schemes/
   │  ├─ services/
   │  └─ repositories/   # 데이터 접근 계층
   ├─ tests/
   ├─ .env.example
   ├─ requirements.txt
   └─ README.md
```

폴더 구조는 구현 과정에서 선택한 인증 및 저장소 기술에 따라 조정할 수 있다.

## 9. 주요 위험 요소와 대응

| 위험 요소 | 영향 | 대응 방안 |
|---|---|---|
| 인증·저장소 방식 미확정 | 인증 및 상품 구현 재작업 | 3단계 시작 전에 기술 선택 확정 |
| 프론트와 백엔드 API 계약 불일치 | 연동 오류와 중복 수정 | 2단계에서 OpenAPI 기준 계약 확정 |
| Gemini API 키 또는 할당량 문제 | 채팅 기능 중단 | 시작 시 설정 검증, 예외 처리, 모킹 테스트 |
| 브라우저 저장소의 인증 정보 노출 | 보안 취약점 | 민감 정보 저장 금지, 만료 가능한 토큰 사용 |
| 동기 외부 API 호출 지연 | 사용자 응답 지연 | 타임아웃, 로딩 표시, 필요 시 비동기·캐시 적용 |
| 의존성 버전 충돌 | 설치 및 실행 실패 | 최소 의존성 정리와 고정 버전 검증 |

## 10. 개발 착수 전 결정 사항

다음 항목은 실제 구현 전에 확정한다.

1. 최종 서비스의 핵심 목적과 주요 사용자
2. 인증 및 데이터 저장소: Supabase, SQLite 또는 기타 저장소
3. 상품 기능의 필수 필드와 사용자별 권한
4. 채팅 내역의 영구 저장 여부
5. 날씨 호출을 프론트엔드에 유지할지 백엔드로 이동할지
6. 배포 대상과 운영 환경
7. MVP 완료 목표와 단계별 일정

## 11. 전체 완료 기준

- 프론트엔드와 백엔드가 환경 변수 기반 주소로 연결된다.
- 회원가입, 로그인, 로그아웃과 인증 복원이 정상 동작한다.
- 상품 CRUD가 실제 저장소를 사용한다.
- Gemini 채팅과 날씨 조회가 오류 처리와 함께 동작한다.
- 인증이 필요한 화면 및 API가 보호된다.
- 핵심 정상·실패 시나리오의 테스트가 통과한다.
- 설치, 실행, 환경 변수, 테스트 방법이 최신 문서와 일치한다.

